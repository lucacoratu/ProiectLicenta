<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WillowClient.Views.SentFriendRequestPage"
             xmlns:viewmodel="clr-namespace:WillowClient.ViewModel"
             xmlns:toolkit="clr-namespace:CommunityToolkit.Maui.Views;assembly=CommunityToolkit.Maui"
             xmlns:m="clr-namespace:UraniumUI.Icons.MaterialIcons;assembly=UraniumUI.Icons.MaterialIcons"
             xmlns:fa="clr-namespace:UraniumUI.Icons.FontAwesome;assembly=UraniumUI.Icons.FontAwesome"
             xmlns:validation="clr-namespace:InputKit.Shared.Validations;assembly=InputKit.Maui"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             xmlns:input="clr-namespace:InputKit.Shared.Controls;assembly=InputKit.Maui"
             x:DataType="viewmodel:MainViewModel"
             xmlns:model="clr-namespace:WillowClient.Model"
             Title="SentFriendRequest"
             BackgroundColor="{AppThemeBinding Light={StaticResource PageLightBackgroundColor}, Dark={StaticResource PageDarkBackgroundColor}}">
    
    <Grid RowDefinitions="Auto,40,*" ColumnDefinitions="*">
        <Border StrokeThickness="0" Grid.Row="0" HeightRequest="40" BackgroundColor="{StaticResource AccentDarkColor}">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="0,0,15,15" />
            </Border.StrokeShape>
            <Grid ColumnDefinitions="*,*,*,*" HeightRequest="40" BackgroundColor="{StaticResource AccentDarkColor}" >
                <ImageButton Grid.Column="0" Margin="20,0,10,0" WidthRequest="24" HeightRequest="24" BorderColor="Transparent" HorizontalOptions="Start" Command="{Binding ExitSentFriendRequestsCommand}">
                    <ImageButton.Source>
                        <FontImageSource FontFamily="FASolid" Glyph="{x:Static fa:Solid.ArrowLeft}" Color="WhiteSmoke"/>
                    </ImageButton.Source>
                </ImageButton>
                <Label FontFamily="{StaticResource ApplicationFont}" Grid.Column="1" Grid.ColumnSpan="2" Text="Sent Friend Requests" FontSize="16" TextColor="WhiteSmoke" FontAttributes="Bold" VerticalOptions="Center" VerticalTextAlignment="Center" HorizontalOptions="Center" HorizontalTextAlignment="Center"/>
            </Grid>
        </Border>
        <material:TextField Grid.Row="1" x:Name="searchFriendRequests" Title="Search sent friend requests" FontFamily="{StaticResource ApplicationFont}" AccentColor="{StaticResource AccentSecondDarkColor}" Icon="{FontImageSource FontFamily=FASolid, Glyph={x:Static fa:Solid.MagnifyingGlass}}" Margin="5"/>
        <CollectionView Margin="0,10,0,0" Grid.Row="2" x:Name="friendRequestsCollectionView" ItemsSource="{Binding SentFriendRequests}" SelectionMode="None">
            <CollectionView.ItemTemplate FontFamily="{StaticResource ApplicationFont}">
                <DataTemplate x:DataType="model:FriendRequestModel">
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems Mode="Execute">
                                <SwipeItem Text="Delete" BackgroundColor="Red"/>
                            </SwipeItems>
                        </SwipeView.RightItems>
                        <HorizontalStackLayout Padding="10" Spacing="6">
                            <HorizontalStackLayout>
                                <toolkit:AvatarView
                                        HeightRequest="50"
                                        ImageSource="{Binding ProfilePictureUrl}"
                                        WidthRequest="50"
                                        BorderWidth="0"
                                        BorderColor="WhiteSmoke"
                                        CornerRadius="32"
                                        BackgroundColor="Transparent"
                                        />
                                <!--<Border VerticalOptions="End" HorizontalOptions="Start" BackgroundColor="#4CBB17" WidthRequest="16" HeightRequest="16" StrokeThickness="1.1" Stroke="#4CBB17" Margin="-15,0,0,0">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="10,10,10,10"/>
                                    </Border.StrokeShape>
                                </Border>-->
                            </HorizontalStackLayout>
                            <VerticalStackLayout VerticalOptions="Center" Spacing="0">
                                <Label FontFamily="{StaticResource ApplicationFont}" Text="{Binding DisplayName}" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" TextColor="WhiteSmoke"/>
                                <Label FontFamily="{StaticResource ApplicationFont}" Text="{Binding About}" FontSize="12" VerticalOptions="Center" TextColor="WhiteSmoke"/>
                            </VerticalStackLayout>
                        </HorizontalStackLayout>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <StackLayout Grid.Row="2" IsVisible="{Binding NoSentFriendRequests}" IsEnabled="{Binding NoSentFriendRequests}" HorizontalOptions="Center" VerticalOptions="Center" Margin="0,0,0,0" Spacing="5">
            <Image Source="no_friends.png" WidthRequest="100" HeightRequest="100" Aspect="AspectFill"/>
            <Label Text="Sorry, you don't have any sent friend requests" HorizontalOptions="Center" TextColor="WhiteSmoke" FontFamily="{x:StaticResource ApplicationFont}" FontSize="14" FontAttributes="Bold"/>
            <!--<Label HorizontalOptions="Center" FontFamily="{x:StaticResource ApplicationFont}" Text=""/>-->
        </StackLayout>
    </Grid>
</ContentPage>